<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:fillAfter="true">
    <scale
        android:duration="300"
        android:fromXScale="0.0"
        android:fromYScale="0.0"
        android:interpolator="@android:anim/linear_interpolator"
        android:toXScale="0.8"
        android:toYScale="0.8"
        android:pivotX="50%"
        android:pivotY="50%"/>

    <alpha
        android:fromAlpha="0.0"
        android:toAlpha="1.0"
        android:interpolator="@android:anim/accelerate_interpolator"
        android:duration="300"/>
</set>

<!-- fab_open

     Inicialmente los dos más pequeños FAB son invisibles
     y no se puede hacer click. Cuando la FAB grande con
     la opción Agregar icono se pulsa la escala FAB dos
     más pequeños, desde su posición inicial invisible y
     al mismo tiempo aumenta la opacidad. Estos dos se
     logra mediante el uso de los elementos de animación
     <escala/> y <alfa/>.

-->